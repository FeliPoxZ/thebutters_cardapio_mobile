import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const background = Color(0xFFF9EDD3);
  static const foreground = Color(0xFF000000);
  static const lightForeground = Color(0xFFF2F2F2);

  // Destaques
  static const banner = Color(0xFFFEBB6F);
  static const highlight = Color(0xFFF7AD59);

  // Identidade
  static const primary = Color(0xFF1C63B2);
  static const secondary = Color(0xFFFF8B2F);

  // Componentes
  static const item = Color(0xFFFDF7ED);

  // Feedback (success / error)
  static const softGreen = Color(0xFF7CCD7F);
  static const onSoftGreen = Color(0xFF18691B);

  static const softRed = Color(0xFFEB6060);
  static const onSoftRed = Color(0xFF5F0707);

  // Extras
  static const extraRed = Color(0xFFF71414);
  static const extraOrange = Color(0xFFC36D20);
}